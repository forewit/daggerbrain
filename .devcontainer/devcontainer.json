{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {},
  "remoteUser": "node",
  "containerEnv": {
    "PATH": "/home/node/.bun/bin:${PATH}"
  },
  "postCreateCommand": "curl -fsSL https://bun.sh/install | bash && bun --version"
}
